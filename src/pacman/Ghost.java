package pacman;

import javax.swing.*;

public class Ghost extends Mover{

	public Ghost(int row, int column, int dRow, int dColumn, boolean isDead,
			ImageIcon image) {
		super(row, column, dRow, dColumn, isDead, image);
		// TODO Auto-generated constructor stub
	}
}
